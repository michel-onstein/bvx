// Deliberately empty.
//
// SwiftPM needs at least one compilable source to treat CVBXEngine as a C
// target; the actual implementation lives in Engine/build/libvbxengine.a,
// which is attached through this target's linker settings in Package.swift.
