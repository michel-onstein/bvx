// Deliberately empty.
//
// SwiftPM needs at least one compilable source to treat CBVXEngine as a C
// target; the actual implementation lives in Engine/build/libbvxengine.a,
// which is attached through this target's linker settings in Package.swift.
